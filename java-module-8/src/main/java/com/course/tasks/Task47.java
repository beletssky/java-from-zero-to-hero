package com.course.tasks;

import java.util.List;

public final class Task47 {

  private Task47() {}

  /**
   * Возвращает все уникальные слова из всех предложений, приведённые к нижнему регистру
   * и отсортированные в алфавитном порядке. Разделитель слов — один или несколько пробелов.
   * Пустые токены отфильтровываются.
   *
   * <p>Примеры:
   * <ul>
   *   <li>{@code uniqueWords(List.of("hello world", "Hello there"))} → {@code ["hello","there","world"]}.</li>
   *   <li>{@code uniqueWords(List.of())} → {@code []}.</li>
   *   <li>{@code uniqueWords(List.of("  a b  ", "b c"))} → {@code ["a","b","c"]}.</li>
   * </ul>
   */
  public static List<String> uniqueWords(List<String> sentences) {
    // TODO: реализуй (только Stream API, без for/if/reduce/parallel)
    throw new UnsupportedOperationException("TODO: реализуй");
  }
}
