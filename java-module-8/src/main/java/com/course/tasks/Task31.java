package com.course.tasks;

import com.course.model.Employee;
import java.util.List;

public final class Task31 {

  private Task31() {}

  /**
   * Возвращает {@code true}, если у каждого сотрудника возраст не меньше 18.
   *
   * <p>Внимание: для пустого списка возвращается {@code true} — это семантика {@code allMatch}
   * ("пустое множество удовлетворяет любому предикату"). Подумай, почему это логично.
   *
   * <p>Пример: {@code allAdults([Employee(age=25), Employee(age=30)]) -> true}
   */
  public static boolean allAdults(List<Employee> staff) {
    // TODO: реализуй (только Stream API, без for/if/reduce/parallel)
    throw new UnsupportedOperationException("TODO: реализуй");
  }
}
